[nginx]
${nginx_ip} ansible_user=${nginx_hostname} ansible_ssh_private_key_file=../../keys/id_rsa_nginx

[backend]
${backend_ip} ansible_user=${backend_hostname} ansible_ssh_private_key_file=../../keys/id_rsa_backend

[app_stack:children]
nginx
backend

[prom_grafana]
${prom_grafana_ip} ansible_user=${prom_grafana_hostname} ansible_ssh_private_key_file=../../keys/id_rsa_prom-grafana
